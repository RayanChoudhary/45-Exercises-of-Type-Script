/*Dinner Guests: Working with one of the programs from Exercises 14 through 18,
 print a message indicating the number of people you are inviting to dinner.*/

let Guestlist = ["Toshi","Arsalan","Irfan"];

// Guestlist.forEach(guestlist => console.log(`${guestlist} today you have invited for dinner at my home`));

let guestlistLength:number = Guestlist.length

console.log(`We are inviting total ${Guestlist.length} guests`)
